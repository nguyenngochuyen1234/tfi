import React from 'react';
import PropTypes from 'prop-types';

Board.propTypes = {
    
};

function Board(props) {
    return (
        <div>
            
        </div>
    );
}

export default Board;