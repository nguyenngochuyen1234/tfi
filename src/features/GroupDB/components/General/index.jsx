import React from 'react';
import PropTypes from 'prop-types';

General.propTypes = {
    
};

function General(props) {
    return (
        <div>
            general
        </div>
    );
}

export default General;