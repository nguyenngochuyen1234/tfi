import React from 'react';
import PropTypes from 'prop-types';

Files.propTypes = {
    
};

function Files(props) {
    return (
        <div>
            Files
        </div>
    );
}

export default Files;